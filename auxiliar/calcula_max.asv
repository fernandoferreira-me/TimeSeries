function [m] = calcula_max(modelo)
%Calcula o tamanho da janela inicial para ajustar as séries

n=size(modelo{6}{1}{2},1)-1;
for i=1:n
maior(i)=nanmax(nanmax(modelo{6}{i}{2}));
end
m=max([max(modelo{3}) max(modelo{5})])+nanmax(maior);%ajuste para equilibrar as amostras retiradas.

end

